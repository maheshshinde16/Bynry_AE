[pytest]
addopts = -ra --strict-markers --junitxml=artifacts/junit.xml
testpaths = tests
markers =
    api: API-only tests
    ui: browser UI tests
    integration: API-to-UI workflow tests
    tenant_isolation: multi-tenant security checks
    smoke: fast critical-path coverage
